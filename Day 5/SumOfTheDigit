class HelloWorld {
    public static void main(String[] args) {
        int n=1234;
        int mod=0;
        int sum=0;
        while(n>0)
        {
            mod=n%10;
            sum=sum+mod;
            n=n/10;
        }
        System.out.println(sum);
    }
}
