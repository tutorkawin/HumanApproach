logic 1:

for(int i=0;i<n.length;i++)
{
	s.out(n[i][i]);
}

logic 2:

for(int i=0;i<n.length;i++)
{
	for(int j=0;j<n[0].length;j++)
	{
    		s.out(a[j][i])
    	  if(i==j)
    		{
    			s.out(n[i][j]);
    		}
    }
}
